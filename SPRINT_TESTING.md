# Klaser — סבב שיפורים: Sprints 1-4 (Testing & Deployment Checklist)

> תאריך: מאי 2026
> טווח Commits: `6f065d0..e2dce19` (8 commits ב-`main`)

---

## 🟥 שלב 0 — חובה לפני בדיקה (תשתית)

### 0.1 הרצת SQL Migrations ב-Supabase

הרץ את שתי המיגרציות **לפי הסדר** ב-Supabase Dashboard → SQL Editor:

1. `supabase/migrations/004_user_preferences.sql` — טבלת סנכרון העדפות (כבר רץ ✅)
2. `supabase/migrations/005_document_assignment.sql` — שתי עמודות חדשות לטבלת `documents` עבור שיוך לבן משפחה

> **למה זה חשוב:** בלי המיגרציות, סנכרון העדפות בין מכשירים ושיוך מסמכים לבני משפחה לא יעבדו.

### 0.2 הוספת `RESEND_API_KEY` ב-Render

1. https://resend.com → צור חשבון חינמי → **API Keys** → **Create API Key**
2. העתק את המפתח (`re_...`)
3. https://dashboard.render.com → Klaser service → **Environment** → **Add Environment Variable**
   - Key: `RESEND_API_KEY`
   - Value: המפתח שהעתקת
4. **Save Changes** → השירות יפרסם מחדש אוטומטית
5. אופציונלי: `RESEND_FROM_EMAIL` עם דומיין שאומת אצלך (ברירת מחדל: `onboarding@resend.dev`)

> **בלי זה:** ניסיון מחיקת חשבון יחזיר 500 ולא יישלח OTP. כל שאר המערכת תעבוד תקין.

---

## 🟦 Sprint 1 — תשתית קריטית

### מה לבדוק

- [ ] **סנכרון בין מכשירים**: התחבר בדפדפן A, שנה צבע אווטר ב-Settings. התחבר באותו חשבון בדפדפן B (incognito) — הצבע צריך להופיע אוטומטית תוך כמה שניות.
- [ ] **זיהוי אימייל קיים בהרשמה**: נסה להירשם עם אימייל של חשבון קיים — אמור להופיע "האימייל כבר רשום" + הצעה למעבר ל-Login (לא הודעת "שגיאה כללית").
- [ ] **ניקיון onboarding**: התחבר → השלם onboarding → התנתק → התחבר שוב — onboarding לא צריך לחזור.
- [ ] **מחיקת חשבון**: User Panel → "מחק חשבון" → תקבל OTP במייל (אם הוספת RESEND) → הזן קוד → החשבון נמחק לחלוטין.

---

## 🟦 Sprint 2 — Onboarding ומסך כניסה

### מה לבדוק

- [ ] **אין flash בכניסה**: רענן את האתר במצב כשאתה לא מחובר. אמור להופיע מסך טעינה כחול עם לוגו ספינר → ואז ה-landing. אסור שתראה את "המערכת" לרגע.
- [ ] **אין המלצות משתמשים**: גלול בעמוד הראשי — סקציית "המלצות משתמשים" עם 3 ציטוטים הוסרה.
- [ ] **Onboarding מותאם**:
  - בחר "אישי/משפחתי" ב-landing → הירשם → אחרי השלמה תקבל onboarding עם 4 שלבים, השלב השלישי "הוסף את המשפחה" (👨‍👩‍👧)
  - בחר "עסקי" ב-landing → הירשם → השלב השלישי "התאם לעסק שלך" (🏢) — הוספת ענף מותאם
- [ ] **Progress bar למגבלת מסמכים**: לא תראה כלום עד 70% מהמכסה. אם תעבור 70% → פס צהוב עם "השתמשת ב-X מתוך Y". מעבר ל-90% → פס אדום.

---

## 🟦 Sprint 3 — Ctrl+K, Bell Badge, Dark Mode

### מה לבדוק

- [ ] **Ctrl+K** (או **Cmd+K** במק): פותח palette במרכז המסך
  - הקלד שם של מסמך → אמור להופיע בסקציית "מסמכים"
  - הקלד "הוסף" → אמור להופיע ב"פעולות" עם "הוסף מסמך חדש"
  - חצים ↑↓ לניווט, Enter פותח, Esc סוגר
  - לחיצה על שדה החיפוש העליון פותחת את ה-palette
- [ ] **Bell — מספר במקום נקודה**: כשיש התראות פעילות, הפעמון מציג מספר בעיגול אדום/צהוב/ירוק (לא נקודה ריקה). 99+ למעלה מ-99.
- [ ] **Dark Mode**:
  - Settings → "מצב כהה" → לחץ על הטוגל → כל הממשק נעשה כהה
  - רענן את הדפדפן → המצב נשמר
  - אם המערכת שלך מוגדרת לכהה (Windows/macOS) ולא בחרת כלום → תיכנס למצב כהה אוטומטית
  - חזרה ל-Settings → הטוגל מסונכרן עם המצב הנוכחי

---

## 🟦 Sprint 4 — מצלמה, FAB, Filter

### מה לבדוק

- [ ] **כפתור מצלמה** (במובייל בלבד, מסכים מתחת ל-900px):
  - לחץ "+ הוסף מסמך" → באזור ההעלאה יש כפתור "📷 צלם מסמך עכשיו"
  - לחיצה פותחת את המצלמה האחורית של הטלפון
  - אחרי צילום, התמונה נכנסת כקובץ ונשלחת ל-AI
- [ ] **FAB במובייל** (מתחת ל-900px):
  - כפתור עגול גדול (60px, צבע אקסנט) בפינה ימנית-תחתונה
  - לחיצה פותחת מודל הוספת מסמך
  - הכפתור "+ הוסף מסמך" בטופבר מוסתר במצב הזה (FAB מחליף אותו)
  - מוסתר במצב לא-מחובר / טעינה
- [ ] **סינון לפי אדם — חוצה רשימות**: עבור לטאב "לפי אדם" → לחץ על chip של אדם → מסמכים בכל רשימות התוכנה (כל הטאבים) מסוננים, לא רק רפואי.

---

## � Sprint 5 — שיוך מסמך לאדם (full-stack)

### מה לבדוק (אחרי הרצת `005_document_assignment.sql`)

- [ ] **הוספת מסמך עם שיוך**: פתח "+ הוסף מסמך" → בחר אדם בשדה "שיוך" → שמור → המסמך נשמר עם השיוך
- [ ] **עריכת שיוך**: לחץ על מסמך קיים → ה-modal של עריכה מציג שדה "שיוך" חדש עם הערך הנוכחי → שנה ושמור
- [ ] **סינון מסנן עכשיו אמיתי**: כשעוברים לטאב "לפי אדם" ובוחרים אדם → רק מסמכים שמשויכים לו מופיעים (לא רק כאלה שאתה מקווה שמשויכים)
- [ ] **רשימה מאוחדת**: chip strip של אנשים מציג את כל בני המשפחה (חדשים ועם אווטרים) + שמות מהמערכת הישנה
- [ ] **רענון בין מכשירים**: הוסף מסמך עם שיוך בדפדפן A → היכנס באותו חשבון בדפדפן B → השיוך מופיע
- [ ] **מחיקת פרופיל לא מאבדת מסמכים**: מחק פרופיל שיש לו מסמכים משויכים → המסמכים נשארים בלי שיוך (לא נמחקים)

## 🐛 Bugs ידועים / מגבלות

1. **Dark mode על מודלים ישנים** — חלק מהמודלים השתמשו בצבעים קשיחים `#fff` ישירות במקום ב-CSS variables. רוב הממשק מכוסה אבל ייתכן שתראה אזורים בודדים שלא מתאימים מושלם. דווח אם זה בולט.
2. **שיוך מסמך — אווטר על הכרטיס** — UI להצגת תמונת/צבע הפרופיל על כרטיס המסמך (`renderProfileChip`) קיים בקוד אבל לא מחובר לתצוגה ב-`makeCard`. שדרוג קוסמטי לסשן עתידי.

---

## 📦 Commit log

```
e2dce19  Sprint 4: camera + FAB + global person filter
6a8d3bc  Sprint 3: Ctrl+K command palette + bell numeric badge + unified dark mode
4c799c7  Sprint 2: per-account-type onboarding + app-loading state + remove testimonials + doc-limit progress bar
6f065d0  Sprint 1: prefs_sync + email exists detection + onboarding cleanup + account delete OTP + (earlier commits)
```

---

## ✅ אחרי שעברת על הכל

אם משהו לא עובד כצפוי, תעלה אלי screenshot + תיאור קצר ואני אתקן.

אם הכל עובד — אפשר להמשיך ל-Sprint 5 (שיוך מסמך לאדם — full stack).
